<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>baxter-config</artifactId>
        <groupId>com.baxter.config</groupId>
        <version>1.1.2-SNAPSHOT</version>
    </parent>

    <artifactId>config-server</artifactId>
    <packaging>war</packaging>

    <name>Baxter Configuration Server</name>
    <description>Baxter Configuration Server web application.</description>

    <build>
        <plugins>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>buildnumber-maven-plugin</artifactId>
                <configuration>
                    <doCheck>false</doCheck>
                    <doUpdate>false</doUpdate>
                    <getRevisionOnlyOnce>true</getRevisionOnlyOnce>
                </configuration>
                <executions>
                    <execution>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>create</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>filter-jetty-resources</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${jetty.resources.dir}</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/jetty</directory>
                                    <filtering>true</filtering>
                                </resource>
                                <resource>
                                    <directory>src/main/scripts</directory>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-jetty-distribution</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>org.eclipse.jetty</groupId>
                                    <artifactId>jetty-distribution</artifactId>
                                    <type>zip</type>
                                </artifactItem>
                            </artifactItems>
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <excludes>*/contexts/*.xml,*/contexts/test.d/**,*/javadoc/**,*/webapps/*.war</excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <inherited>false</inherited>
                <executions>
                    <execution>
                        <id>create-bundle</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                        <configuration>
                            <descriptors>
                                <descriptor>src/main/assembly/bundle.xml</descriptor>
                            </descriptors>
                            <attach>true</attach>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>Package-Unix</id>
            <activation>
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>unix-maven-plugin</artifactId>
                        <configuration>
                            <debug>false</debug>
                            <revision>${buildNumber}</revision>
                            <contact>Baxter</contact>
                            <contactEmail>info@baxter-it.com</contactEmail>
                            <description>${project.description}</description>
                            <assembly>
                                <mkdirs>
                                    <paths>
                                        <path>${installation.unix.home.dir}</path>
                                        <path>${installation.unix.home.dir}/contexts/config-server</path>
                                        <path>${installation.unix.home.dir}/repository</path>
                                    </paths>
                                </mkdirs>
                                <copy-file>
                                    <path>${jetty.resources.dir}/config-server.xml</path>
                                    <toDir>${installation.unix.home.dir}/contexts</toDir>
                                    <attributes>
                                        <mode>664</mode>
                                    </attributes>
                                </copy-file>
                                <copy-file>
                                    <path>${jetty.resources.dir}/override-web.xml</path>
                                    <toDir>${installation.unix.home.dir}/contexts/config-server</toDir>
                                    <attributes>
                                        <mode>664</mode>
                                    </attributes>
                                </copy-file>
                                <copy-file>
                                    <path>${project.build.directory}/${project.build.finalName}.war</path>
                                    <toDir>${installation.unix.home.dir}</toDir>
                                    <attributes>
                                        <mode>664</mode>
                                    </attributes>
                                </copy-file>
                                <copy-directory>
                                    <from>${unpacked.jetty.dir}</from>
                                    <to>${installation.unix.home.dir}</to>
                                </copy-directory>
                                <copy-directory>
                                    <from>${jetty.resources.dir}</from>
                                    <to>${installation.unix.home.dir}/bin</to>
                                    <includes>
                                        <include>*.sh</include>
                                    </includes>
                                    <fileAttributes>
                                        <mode>775</mode>
                                    </fileAttributes>
                                </copy-directory>
                            </assembly>
                            <deb>
                                <priority>optional</priority>
                                <section>${installation.group.name}</section>
                            </deb>
                            <rpm>
                                <group>${installation.group.name}</group>
                            </rpm>
                        </configuration>
                        <executions>
                            <execution>
                                <id>build-deb-installer</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>package-deb-attached</goal>
                                </goals>
                            </execution>
                            <execution>
                                <id>build-rpm-installer</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>package-rpm-attached</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>

                </plugins>
            </build>
            <properties>
                <installation.home.dir>${installation.unix.home.dir}</installation.home.dir>
                <installation.unix.home.dir>/opt/baxter/config-server</installation.unix.home.dir>
                <installation.unix.dist.dir>${installation.unix.home.dir}/dist</installation.unix.dist.dir>
                <installation.group.name>Baxter Configuration Server</installation.group.name>
            </properties>
        </profile>
    </profiles>

    <dependencies>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>config-om</artifactId>
        </dependency>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>config-processor</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-log4j12</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>commons-io</groupId>
            <artifactId>commons-io</artifactId>
        </dependency>
    </dependencies>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <unpacked.jetty.dir>${project.build.directory}/jetty-distribution-${jetty.version}</unpacked.jetty.dir>
        <jetty.resources.dir>${project.build.directory}/jetty-resources</jetty.resources.dir>
        <jira.component>${project.artifactId}</jira.component>
    </properties>

</project>
